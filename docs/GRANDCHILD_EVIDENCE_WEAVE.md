# Grandchild evidence Weave

`integrations/artifacts/grandchild-evidence` is a compact external provenance
graph for the completed rich grandchild batch. It links the update-160 checkpoint,
the authenticated native V3 export, the audit of all 491,520 transitions, and the
native GAM atlas. The atlas branch retains two fitted laws and the failed
goal-progress fit; the failed fit minted no model.

The audit records mouth-material contact in 90 of 96 resident trajectories and
energy loss in all 96. The graph makes no matched-control or behavioral-improvement
claim. Its two coefficient arms, `0.01` and `0.001`, are a proposed paired
continuation from the same checkpoint. Neither arm has run, created a checkpoint,
or started a resident.

Predictive Courtyard V2 is a disconnected life branch. Its last complete checkpoint
is referenced by SHA-256 from outside Git at tick 9,021. A later physical-source
range error paused the world at a read-only time of 458.9 seconds; the incomplete
tick was not saved and no continuation is recorded.

Regenerate on the machine retaining the external Courtyard checkpoint:

```bash
python3 integrations/export_grandchild_evidence_weave.py
```

The exporter hashes every repository source, authenticates the external checkpoint,
then invokes the pinned native Universal Weave adapter. The portable receipt records
successful serialization, reload, and DAG validation. Large training and habitat
checkpoints remain outside Git as content-addressed blob references.
