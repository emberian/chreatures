# Ecological specialization: quiescent handoff

The user requested winding toward quiescence after implementation commit
`ad90191d76b12e0db299ccedf98e62b302f593c0` was pushed. No new full-neural world,
655,360-transition collection, or AMD refit was launched. Resume deliberately
from current source after reading the remaining integration work below.

## Landed mechanisms

- Native region network: finite seven-pool inventories in ordinary metabolic
  rows; physical route clearance, shared-prestate competing flows, conservative
  outlets, complete packet exit transfer and physical retirement.
- Native metabolic acclimation: inherited bounded enzyme response, relaxation
  and ATP cost; current expression and expenditure remain private state.
- Native resident v7: 32 private executed-sequence slots, four local plus up to
  four recalled proposals, one H8 recurrent prediction batch, actual-length
  scoring, generation-safe attribution and exact private snapshots.
- Current genome/search v3 and regional-residents-v3: cold inherited regulation
  diversity; regional grammar v4, biosphere birth v7 and snapshot v9.
- Corpus v4: fixed 512-tick disk-backed shards, 1,024-tick/end coupled snapshots,
  authenticated progress relay, manifest published last, whole-world partitions.
- Recorded observer v3: regional pools and actual directional flows, local and
  recalled action candidates. Existing actual v6 recording remains the default.

## Executed evidence

The v7 artifact passed a ten-boundary native B1 integration with eight acquired
slots, four recalled candidates and exact next-decision restoration across all
63 result fields. This is execution evidence, not learned physical skill.
A fresh generated articulated B8 world constructed with 413 geoms, 151 chemical
rows, 21 material regions, 29 routes and six outlets, then advanced actual
physics and biosphere together. The extended joined assay used an earlier isolated `gen8` native build and
stopped before its 1,200-tick continuation or restore check. Under that pinned
build all routes were already blocked. A separate one-tick startup using the
current `b33598e6` native build sampled routes with accessibility up to 0.8. Blocking all six
outlets with physical objects retained inventory, but a designated finite deposit
did not activate a free packet, so the exit case could not proceed. It used
analyst actions, not a neural policy. See
`data/ecology/regional-ecology-v1.receipt.json`; the exact inputs/native binary
are retained under `/Users/ember/paperbin/chreatures/integration/ecological-specialization-v7-incomplete`.

**First integration task on resume:** pin the joined assay to the current
complete native build, then inspect the selected outlet's placement and physical
deposit result. The current startup already demonstrated open routes; the failed
older-build assay does not establish that a source repair is necessary. Establish
a finite successful outlet, then complete the exit, long continuation and
restoration checks before expensive collection.

Native GAM fitted the preceding 160 completed life observations (eight residents
shared each world). Grouped partitions had 75 training, 37 validation and 48
final reporting rows. Energy prediction improved slightly; other responses did
not pass both comparisons and no tensor interaction was promoted. The atlas
rejects the new mechanisms as out of support. Native Universal Weave preserves
the two campaigns and the atlas as three nodes with three edges, including a
multi-parent fit record. See `GENOTYPE_ENVIRONMENT_ATLAS.md` and
`integrations/gam/artifacts/ecological_transfer_atlas_v1/`.

## Queued compute, not running

Use isolated current-source builds. Do not reuse a frozen resident's loaded
engine or restore an old life into these new formats.

The initial collection is ten worlds × eight residents × two episodes × 4,096
physical ticks = **655,360 resident transitions**, seed 20260919. World slots
0–7 train, 8 validate, and 9 supply final holdout. Prior collection throughput
suggests roughly 48 minutes before changed-mechanism costs; this is an estimate.
Bulk output: `/tank/chreatures/runs/collections/rich-sensorimotor-play-v4-seed20260919`.

Before launch, build the current native extensions on hbox, materialize and pin
the current profile, and create the matching **80 genome-v3 candidate bank**.
Neither the profile nor this bank was materialized during shutdown. Then use
`scripts/collect_rich_sensorimotor.py`, with regional-v4 grammar and the current
schedule, tiled AMD brain, fast physical backend, W10/R8/episodes2/steps4096.
Scope `HSA_OVERRIDE_GFX_VERSION=10.3.0` to hbox's RX 6750 XT process; keep caches
and data under `/tank`. The relay in `docs/COMPUTE.md` sends sealed files to
`/home/ember/chreatures-data/sensorimotor-play/rich-v4-seed20260919` on persvati.
The trainer waits for the final authoritative manifest.

Native controller artifact:
`/tank/chreatures/scratch/cognitive-v7-integration/artifacts/developmental-resident-population-v7.npz`

- File SHA: `6f6a07c9042786704e43ef7fe10fca39871e8a19d25c5a34695e3e398eecf3c5`
- Internal identity: `ec690f724c2f920140eee145365b87d07ed392bbe4cae97225db0f79519277c3`
- Frozen Torch **v5**, not v7, source:
  `/tank/chreatures/runs/development/population-v5-body-actuator-seed20260917-attempt4/development.pt`
- Source SHA: `904c72f2d3a310c7b1262d39777ec1de9a3cd0d6a7c2b1aa2a11637106d84f22`

The recurrent predictor remains format v3 with context 1,560 and H8. The
representation and base weights are inherited priors; they were not refitted on
the new regional dynamics. Future persvati fitting uses its existing native
ROCm7 environment and pinned validation/holdout slots, followed by native export.

## Staged M2 world and publication

The inert deployment is
`/Users/ember/paperbin/chreatures/deployments/ecological-specialization-v7`.
Ports 8786/18786 were checked free. Launch scripts require source, profile,
canonical birth and native identities before changing PENDING to READY.
No services were started. The unused new archive job was booted out; its
`ENABLE_ARCHIVE_COMMAND.txt` records how to re-enable it. Existing archive jobs
remain operational to preserve previous lives and manage disk.

After an explicit resume: finish the joined integration, freeze source and
canonical birth, re-enable the scoped archive, launch the dedicated 32-slot full
MaleCNS service and eight founders, then capture an actual v3 recording. Check
its material and acquired-action paths with Node/HTTP tools before replacing
the public default. No browser automation is needed.

The original reciprocal-v6 branch remains paused at tick 9,170 with coherent
checkpoint 9,148 preserved. No automatic retry, coordinate clamp, state merge,
or silent engine migration was performed. New region exits address a physical
lifecycle mechanism; they do not establish that the old failure has been
reproduced or that a sustained food web now exists.
