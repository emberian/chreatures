# Reciprocal ecology development wave

Approved after the population wave and observatory repair at `9ad2139`.
The implementation objective is to make personal learning, bodily action,
development and ecological inheritance affect one another in the same runtime.
This is a substantial coordinated build followed by a joined research run;
working modules and planned behavior remain distinguishable throughout.

## Shared organism boundary

The organism interface remains v4: twelve delivered actions, twelve physiology
channels, 4,459 permitted observation values, and 384 MaleCNS readouts. The rich
visual field remains 1,024 body-local rays. Names, global coordinates, world
graphs, experiment archives and caregiver identities are not sensory inputs.
Native resident artifacts/execution advance to v6; existing lives retain their
frozen source and artifacts. Torch policy weight shapes remain v5. A new native
birth explicitly combines authenticated inherited policy weights with the new
predictor and private-memory mechanisms. Old predictors are not a fallback.

## Coupled implementation

* **Full-body prediction:** a three-member recurrent ensemble receives the four
  experienced 256-dimensional frame codes, private 128-dimensional effective
  worker context (GRU state plus the inherited-gain adaptation state),
  384 neural readouts, twelve current physiology values and twelve previous
  delivered actions (1,560 context values). It forecasts a proposed suffix of
  one to eight twelve-axis actions at 0.05 seconds per step. Each step predicts
  256 frame-code deltas and twelve physiology deltas. A smooth headroom-bounded
  link is trained and executed identically, so physiological prediction remains
  within the declared channel ranges without rejecting every small residual at
  an empty reserve. This is a surrogate response law, not conserved chemistry.
  Predictions do not update
  experienced state. Training uses contiguous executed suffixes, whole-world
  partitions and an authenticated frozen representation. Native inference uses
  batched matrix operations and bounded preallocated scratch space. macOS dense
  matrix products use Accelerate; Linux retains the SIMD matrixmultiply kernel.
  The live planner compares four-tick constant-action hypotheses, delivers one
  tick, and re-evaluates after the next actual observation.
* **Personal sequence memory:** sparse private edges connect exact achieved-goal
  slot generations. Attempt, attainment and timeout remain distinct. Bounded
  contextual path proposals influence goal selection; past succession is not a
  guarantee of reachability. Replacement invalidates incident memories. Local
  consolidation operates on experienced transitions, and all learned state and
  scheduling state are checkpointed and initialized afresh at birth.
  The 128-slot achieved-history store now divides capacity between 64 lifetime
  reservoir entries and 64 recent entries. After filling, a changed sensory key
  (RMS at least 0.08) or ten elapsed ticks permits admission. This keeps old
  residents able to acquire new encounters; it does not manufacture successful
  goals or transfer an adult's memories to offspring.
* **Material interdependence:** conserved fermentate and balanced reaction
  pathways create additional enzyme-budget tradeoffs. Finite physical packets
  and contact carry resources; scent fields carry explicitly nonconserved cues.
  Shared chemistry supports cross-feeding without assigning species roles.
* **Local development:** inherited growth rules receive actual surface,
  clearance and resource cues. Parent-part ancestry, attachment and path
  conductance affect resource-funded branch/root/leaf development. Committed
  geometry changes affect collision, illumination and contact transport.
* **Organism–environment search:** current search-v2 records pair histories,
  environment-conditioned niches, direct transfers and challenge proposals.
  Engine faults are separate from ecological outcomes. Environment ancestry
  and failed/partial lives remain available to future proposals. GAM response
  surfaces may guide analyst search with authenticated provenance.
* **Native GAM:** fit history/state/action-sensitive energy, fatigue and effort
  laws against actual cohort histories, with declared support and bounded action
  score effects. Report whether action dependence actually differentiates
  proposals. Fitted associations do not become measured physiological laws.

## Interaction and observatory

World state exposes a bounded journal of committed evidence events with stable
sequence, physical tick, model time, body/entity references, quantities and
source. Event kinds derive from actual mechanisms, not from inferred intentions.
The headless public recording advances to v2 with multi-resident details and
explicit stream availability. Viewers may read archived v1 recordings; this does
not add a second organism runtime. Selection, gaze, movement, signals, material
exchange, growth and learned-memory summaries must be traceable to recorded
states. Viewing or navigating evidence does not mutate a life.

## Ownership and convergence

Root owns creative architecture, native resident integration, host boundary,
coupled persistence, event publication, commits and joined acceptance. Deputies
own prediction/training, private sequence memory, ecological chemistry, local
growth, coevolution, GAM, recording/Weave, public observatory and compute. Shared
file edits are coordinated. Compile/interface checks support the substantial
build; the principal evaluation follows integration and repairs failures that
matter to the joined runtime. Work is headless on M2 and the AMD nodes, with bulk
artifacts under `/tank/chreatures` and all existing lives preserved.
