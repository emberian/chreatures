# Trained population-v5 research run

The first isolated trained population-v5 M2 run uses source revision `ee18250`, eight fresh residents, the full 165,122-neuron MaleCNS graph, the update-20 controller, and the corrected two-law population-response bank. The public capture and compact receipt preserve the exact controller, native-library, graph, bank, world, and recording identities.

The visitor sequence transferred a finite reserve-fruit portion at coordinates derived only from static habitat bounds, then delivered two signal and two light events. No resident position, identity, or private state selected those coordinates. The run remained unpaused and the eight residents advanced throughout the capture.

The population-response counters report whether executed transitions were inside the fitted law domain. They do not show that the bank caused an action choice. Likewise, private update counts establish that native personal state changed; they do not establish competence or improvement.

This particular frozen research service has eight neural slots, all allocated.
It cannot add a ninth neural resident. Future ecology deployments should reserve
32 slots at startup. Current source now defers funded hatching when the neural
service is full, preserving the brood without attempting a failed allocation;
that later guard was not installed into this frozen life.

[Watch the actual trained-organ recording](https://emberian.github.io/chreatures/living.html?recording=trained-organs)
or open the running local world at `http://127.0.0.1:8783`.

See `data/development/population-v5-trained.receipt.json` for the compact authenticated record. The full private raw recording and neural snapshots remain in the operator deployment/archive; the public recording is sanitized separately.
